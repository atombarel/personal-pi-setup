{
  "name": "personal-pi-setup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pi": "pi",
    "doctor": "node scripts/doctor.mjs",
    "check": "node scripts/check-config.mjs && pi --version",
    "models": "pi --list-models",
    "models:core": "pi --list-models openai-codex && pi --list-models github-copilot && pi --list-models openrouter",
    "login": "pi"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.8"
  }
}
